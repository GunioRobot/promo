<%= form_for(@affiliate_network) do |f| %>
  <% if @affiliate_network.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@affiliate_network.errors.count, "error") %> prohibited this affiliate_network from being saved:</h2>

      <ul>
      <% @affiliate_network.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
